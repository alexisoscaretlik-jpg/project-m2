# reports/twitter

Owner: `twitter-curator`. Daily log of the `cron/fetch-tweets` pipeline.

Filename: `YYYY-MM-DD.md`.

Each entry: rows fetched, top-engagement tweet ids, sentiment-tag pass results, anomalies (auth errors, source quality drift), action items.
