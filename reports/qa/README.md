# reports/qa

Owner: `qa`. One file per QA pass.

Filename: `YYYY-MM-DD-<ticket>.md`.

Each entry: ticket reference, PASS/FAIL per static check (typecheck, lint, build), PASS/FAIL per browser smoke route (desktop + mobile widths), screenshots paths for visual issues, console/network excerpts for any FAIL.
