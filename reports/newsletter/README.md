# reports/newsletter

Owner: `newsletter-operator`. One file per send (or attempt).

Filename: `YYYY-MM-DD.md`.

Each entry: subject used, candidates considered, sent/failures/total counts, open rate (24h post-send if available), notable failures, link to preview HTML if archived.
