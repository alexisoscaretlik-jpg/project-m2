## 2026-04-25

- Cron: **ok** (recovered after server restart; initial attempts 500'd — see §Infra incident)
- Seen: 15 | New: 5 (3+2 across two runs) | Saved: 5 | Errored: 0 (1 transient `empty` → saved on retry)
- Errors: [] *(no persistent errors)*
- Backlog estimate: 0 (RSS returned 15 videos; all unprocessed ones are now saved)

### Run log

| # | Time (CEST) | seen | new | saved | notes |
|---|-------------|------|-----|-------|-------|
| 1 | 00:17:57 – 00:21:31 | 15 | 3 | 2 | pvll7speEgI returned `empty` |
| 2 (retry) | 00:21:43 – 00:30:06 | 15 | 3 | 3 | pvll7speEgI saved; +2 backlog cleared |

### Videos distilled

- `0Imbf5xVrwk` — *TRUMP IS PISSED: PREPARE FOR TUESDAY* — saved (run 1)
- `7705qDZGYPw` — *This Changes Everything.* — saved (run 1)
- `pvll7speEgI` — *So WTF now...* — empty on run 1, **saved on retry**
- `-ZhsNF9gID0` — *Trump JUST **Announced** Big Iran U-Turn* — saved (run 2)
- `DglqEYPmZmg` — *This Changes EVERYTHING for Stocks.* — saved (run 2)

### Trigger details

- Endpoint: `http://localhost:3000/api/cron/watch-kevin`
- Auth: `Bearer $CRON_SECRET` (read from `web/.env.local`)
- Attempts: 3 (20:55:35, 20:56:10, 20:56:53 CEST)
- All returned `HTTP/1.1 500 Internal Server Error`, `Transfer-Encoding: chunked`, empty body.

### Server-side context

`web/logs/stderr.log` was last written at 20:56:10, matching one of the calls. The recent stderr lines all show the same Next.js 16 / Turbopack chunk-load error pattern:

```
Error [ChunkLoadError]: Failed to load chunk
  server/chunks/[root-of-the-server]__0xbvjrg._.js from module 62918
[cause]: Cannot find module
  '.next/server/chunks/[root-of-the-server]__0xbvjrg._.js'
Require stack:
  - .next/server/chunks/[turbopack]_runtime.js
  - .next/server/app/api/cron/fetch-tweets/route.js
```

The require-stack entries name `fetch-tweets/route.js`, not `watch-kevin/route.js`, so the watch-kevin failure may be a sibling chunk that was emitted alongside the fetch-tweets one. The current `.next/BUILD_ID` is dated Apr 25 15:18, but the missing chunk (`__0xbvjrg`) was apparently pruned or never written. Fix is almost certainly a clean rebuild of `web/`:

```
cd invest-coach/web && rm -rf .next && npm run build
```

This is out of scope for yt-distiller (build/infra). Escalating to @ceo on INV-8.

### Failure recovery

- Per HEARTBEAT.md §4, single-result errors get one retry then a skip. Here the failure is endpoint-level, not per-result. No videos were attempted, so there is nothing to retry per-id. No `private_notes` were touched.
- No quota or auth signals — request authenticated cleanly (no 401/403), and we never reached Gemini.

### Resolution

Server was restarted (by product-builder per [INV-20](/INV/issues/INV-20), since cancelled; restart appears to have cleared the stale chunk without a full rebuild). Route responded 200 on the third attempt with a 600s curl timeout. No further infra action required.

### Next actions

- First weekly quality sample due **Mon 2026-04-27** → `reports/yt-distiller/samples/2026-W17.md`.
