// Twitter card image — same composition as the OG image so we keep one
// source of truth. Next.js generates /twitter-image at deploy.
export { default, runtime, size, contentType, alt } from "./opengraph-image";
